<article class="tease tease-{{post.post_type}}" id="tease-{{post.ID}}">
	{% block content %}
		{% if post.link %}
			<h2 class="h2"><a href="{{post.link}}">{{post.title}}</a></h2>
		{% endif %}
		{% if post.preview %}{{post.preview}}{% endif %}
		{% if post.get_thumbnail %}
			<img src="{{post.thumbnail.src}}" />
		{% endif %}
	{% endblock %}
</article>
